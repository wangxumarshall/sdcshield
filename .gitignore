*~
/local.md
/sdcshield-*.log
/sdcshield-*.yaml
/total_log_output.yaml
# forever-patrol loop artifacts (test loop run artifacts, not source)
/run-all-forever.log
/run-pid.txt
# typical names for meson build directories
/build*/
# offline-build runtime artifacts (generated, not source):
#   dist/            — 现场 tarballs (package-release.sh 产物)
#   built-index.tsv  — 跨 SP 总表 (package-release.sh 每次重新生成)
dist/
/scripts/offline-build/built-index.tsv
# ctags
tags
/.vscode/
# Claude Code local artifacts (session state, agent memory)
/.claude/
/memory/
# oversized raw field logs kept on disk, never for git (245 MB):
/docs/cases/hpc/Linsheng 10.39.0.1_2026-09-11_6_42_28.log
# standalone parent of the split sve512 workload tests, intentionally
# untracked until it gets its own meson entry (see sve512_*_arm.cpp):
/tests/cpu/arm64/sve512_fma_arm.cpp

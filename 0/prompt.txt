A session-scoped Stop hook is now active with condition: "所有15个openeuler os的sdcshield 二进制都要构建，并且push到对应的 submodules对应的目录中,并分别验证这15个二进制，都能在对应的os镜像中测试通过。". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.
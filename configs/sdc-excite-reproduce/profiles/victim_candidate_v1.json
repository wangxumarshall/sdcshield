{
  "_comment": [
    "victim_candidate_v1 — M3 候选 victim 激发 profile 模板（v5 §7.5 语义）。",
    "JSON 裁定（M3 计划 Tech Stack）：v5 §7.5 的 YAML 示例为语义规范，本文件是其",
    "语义等价 JSON 改写——零第三方依赖（不引 yaml）；JSON 无注释语法，\"_\" 前缀",
    "键为注释载体，sdc_profile.load_profile 校验时忽略。",
    "字段与 v5 §7.5 逐项对应：schema_version/profile_id/victim/aggressors/",
    "environment/monitoring/safety_policy/limits；victim.cpus 按目标机 topology 修改",
    "（本模板取 96——resolve 会拒绝不在 online 集的编号，禁止 p0 假定）。",
    "受界纪律（M3 Global Constraints，战役并行运行时）：单次 ≤5min",
    "（limits.duration_s=300）、aggressor 总并发 ≤16 线程（aggressors.max_threads，",
    "run 期按 family 数均分）、--cpuset 限界核集； governor 恒 performance",
    "（实验档需独立授权——v5 §7.4.1/T2 裁定，resolve 拒绝非 performance）。",
    "aggressor family→测试名默认映射见 tools/excite/sdc_profile.py 的",
    "DEFAULT_FAMILY_TESTS（--list-tests 实测名）；可用 ProfileRunner.",
    "_tests_for_family 覆写。用法：python3 tools/excite/sdc_profile.py run <本文件> --dry-run。"
  ],
  "schema_version": 1,
  "profile_id": "victim_candidate_v1",
  "victim": {
    "cpus": [96],
    "tests": ["openblas_dgemm"],
    "seed_policy": "fixed",
    "iterations_per_seed": 100
  },
  "aggressors": {
    "topology": "all_except_victim",
    "families": ["cache", "integer"],
    "duty_cycle": 1.0,
    "max_threads": 16
  },
  "environment": {
    "governor": "performance",
    "numa_policy": "recorded_default"
  },
  "monitoring": {
    "pmu_groups": ["core_base"],
    "steady_period_ms": 1000
  },
  "safety_policy": "lab_default_v1",
  "limits": {
    "duration_s": 300,
    "max_failures": 3
  }
}

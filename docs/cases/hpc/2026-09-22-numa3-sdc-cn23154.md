# HPC 案例:cn23154 NUMA3 SDC 独立复现研究(2026-09-22 起)

> 状态:进行中(过夜队列运行中;结果章节将随数据落地更新)
> 材料目录:`/home/share/suke/0903-NUMA3-report-materials-wangxu`(本案例全部结论仅基于该目录内数据)
> 工作目录:`ai-run-cn23154-20260922-182127/`(RES;协议见材料目录 CLAUDE.md)

## 1. 背景与任务

- 现象(历史材料记载):材料方在 cn23154(鲲鹏 920F,608 核,32 NUMA 域 = 16 DDR + 16 HBM)运行第一性原理 DFT 类应用(xlsdft/chefsi 体系,16 MPI rank × 36 OpenMP 线程)时,出现 SVE `ld1d` 指令 SIGSEGV,疑似 NUMA3(核 114-151,重点 core139)相关 SDC。
- 任务:独立复现(不预设结论)、core139 及 NUMA3 全核 + 608 核对照、充分实验、微架构级诊断、最小稳定压测用例。
- 约束:只用材料目录内数据与复现程序;不得改动系统(BIOS/微码/重启/全局内核参数/频率策略);结果可追溯;诚实记录。

## 2. 环境独立测量(结论与前提的出入)

| 项 | 前提/传闻 | 独立实测 | 出处 |
|---|---|---|---|
| 主频 | "已恢复 2GHz" | **锁定 ~1.55GHz**(userspace governor,setspeed=cur=1.549GHz,负载下不变;2.0GHz 仅为能力上限) | environment/freq-check.txt, job 1694190 |
| 拓扑 | NUMA3=核 114-151 | 确认(每域 38 核);HBM 域 = DDR 域 n+16;app rank r 大缓冲 bind 到 die-local HBM(rank3→NUMA19,~3.0GB) | numa_maps 快照 |

**含义**:历史现象若真实,其复现条件与当前 1.55GHz 锁频环境存在系统性差异(历史时点频率未知);阴性结论必须以此限定。

## 3. 方法

- **复现程序**:材料目录应用本体(16×36),BIND_MODE=ppr / rotate(shift 0-15),watchdog 2400s,逐 run 线程/频率/numa_maps 采样,run-results.jsonl 结构化记录(~25 字段)。
- **阅读顺序协议**:先源码 → 独立基线 → 之后才允许对照历史日志(避免先入为主)。
- **最小压测用例 mini_laplacian**(自研,材料目录内构建):从 `stencil.cpp:1695 calc_laplacian_d3_c2_o0<double,double>` 忠实提取内核(FDn=12/ghost=12/omp static-chunk+simd),Si.inpt 几何折半;黄金参考=同函数 1 线程计算(逐位相等);每迭代 memcmp + 巡检轮转 FNV + 金丝雀 + 信号捕获;退出码分类 clean/SDC(3)/crash(4)/canary(5)/checksum(6)/placement(7)。

## 4. 主要发现(按时间序)

### 4.1 轨迹确定性证明
iteration-1 scf error = 1.812e-01 与历史(崩溃)日志**逐位一致** — 当前环境完整重走历史轨迹的计算路径。

### 4.2 阴性基线(截至 2026-09-23 00:00,1.55GHz)
- app 级 12 跑 12 干净:ppr×6 + rotate×4 + FULL608(NT=38)×1 + rot-s0-long(**89 SCF 迭代/2h/零异常**,历史崩溃点 iter 2 的 44× 裕度)。
- 统计上界(Rule of Three,95%):n=12 → p≥25% 强复现假设被拒(上界 ~22%);弱化结论待 stats20(n≈32)。
- mini 级:仪表验证冒烟 ~3100 迭代 0 误报(cores 150/151)。

### 4.3 工具链发现:BiSheng libomp 跨 omp single 可见性缺陷(实证)
- 现象:普通共享变量(int rot/long n_checks)跨 `#pragma omp single` 边界读到陈旧值(非单调、倒退);atomic 变量完全一致。
- 证据:smoke-v4 verbose 巡检日志(rot 倒退跳变;n_checks 记 12 vs 实际 95;v3 golden 假阳性=读到陈旧 sum_golden0=0)。
- 工程后果:该节点上任何 OpenMP 仪表/工具的跨 single 标量必须 atomic;**原应用同工具链编译,存在同类敏感点**(见 4.4)。

### 4.4 原应用崩溃路径竞态审计(19 点,两层)
- 真实崩溃调用链:`Aar<double>::run_ompunnested_mp`(+0x273e54/+0x274428,内存池版)→ `calc_laplacian_d3_c2_o0` → SVE ld1d。
- **OpenMP 同步层**:全部写→读路径 barrier 覆盖;matrix_product_general 按输出分布+拥有者线程内归约;MPI 全 master 串行化(FUNNELED 合规);FFTW per-tid plans — 规范安全。
- **内存池层**:Memory_pool 为无锁 bump 分配器(类本身非线程安全),但崩溃路径全部调用串行;`cal_Gamma_ompunnested_mp` 有 `omp_in_parallel()` 运行时守卫;**asserts 激活(二进制含 __assert_fail)→ 池溢出必 SIGABRT ≠ 观测的 ld1d SIGSEGV — 机制排除**。
- 结论:H2b(软件竞争/池缺陷致崩溃)在崩溃路径两层排除;H1(硬件异常)为唯一主要假设;H2a(工具链可见性,影响控制标量 r_2norm/tol/total_Sum)独立存续、后果有界(收敛控制,非数据损坏)。
- 详见 RES/diagnostics/omp-mpi-race-audit.md。


### 4.5 历史四崩溃微架构分析(2026-09-23 00:2x,决定性)

**RANK_BIND 铁证**:四次崩溃(ppr.shift0 / rotate.shift0/1/2)的进程亲和性全部 = cpus 114-149
(die3/NUMA3,含 core139)。崩溃跟随 NUMA3 域而非 rank 编号:ppr/rot0 崩 rank3、rot1 崩 rank2、
rot2 崩 rank1 — 三种 rankfile 布局下崩的都是被绑到 114-149 的进程。历史数据定位止于 die 级。

**故障地址位解剖**(四地址完全同构):
- bits[47:32] 全部 = 0x4001;bits[31:0] 各异;bits[63:48] = 垃圾(0x000C/0x0036/0x25A6/0x0004,
  无 stuck-at 一致性)。
- 低 48 位经活进程 /proc/PID/maps 实测验证:落入各 rank 的 5.00GB Memory_pool 大区
  [0x4000_f3e0_0000, 0x4002_33e0_0000] 内部(+6.05..+7.00GB,>2GB 裕量)。
- 即 **故障地址 = 池内有效地址 + 垃圾高 16 位** — 若无高位损坏,加载本应正常完成。

**指令判读**:`ld1d {z17.d}, p1/z, [x11, x10, lsl #3]`(+0xa1a28)/`ld1d {z16.d}, p1/z, [x14, x10, lsl #3]`
(+0xa1a64);+0xa1a24 处共用变址 x10 的前一加载成功 → 索引有效、**基址寄存器 x11/x14 高 16 位损坏**。

**位宽论证**:32 位网格索引(即使被污染)sign-extend ×8 只能产生 ±2^34 偏移,无法置起 bits[63:48]
→ 排除 32 位循环变量类软件解释;libomp 边界栈损坏同理被位宽排除。软件侧与 19 点审计共同收敛到
**H1a:64 位 GPR(基址)高 16 位损坏 — 寄存器堆/AGU 级硬件缺陷或瞬态**。

时序:四崩溃均在 SCF 第 2/3 轮(iter1/iter2 误差打印后);本 run 已逐位相同地越过该窗口 12+ 次。

详见 RES/diagnostics/historical-crash-microarch-analysis.md。

### 4.6 节点 coredump 取证:72 个 core 全部为人为注入产物(2026-09-23)

节点 /var/lib/systemd/coredump 存 2026-09-20 19:41 → 09-22 02:34 的 72 个 core(uid 28370):
a.out×28(全 SIGABRT 池断言,16 rank 同秒两簇)、/usr/bin/sort×35(pc=0、code=1 真实内核故障、
寄存器跨 2.5 天逐样本同构)、xlk_min4×6(handler re-raise 现场污染)。

**决定性**:完整 NT_FILE 核查发现 a.out 与 sort 族(首末样本均确认)预加载了
/home/share/suke/sdc-allcore-20260920/**sdcfault.so**(另一力度的故障注入库);sort 崩溃的调用点
(lr)与目标(x17)均落在 sdcfault.so 映射内 → 72 个 core 全部为受控注入实验产物,
**该窗口节点零自发崩溃**。

方法学更正(如实记录):sort×35 曾被初判为"系统性节点不稳"的独立证据,核查后撤回 —
崩溃证据必须先验来源(进程映射/预加载/二进制指纹)再归因。详见 RES/diagnostics/node-coredump-inventory.md。

## 5. 结果

### 5.1 matrix1/2 + 对照(app 级,已收官 2026-09-23 03:00)

job 1695152/1695153 完成。**34 条记录 = 29× clean_iter5 + 2× oom_hbm_pairing(已归因排除) + 1× timeout(rot-s0-long 2h 长跑,非崩溃) + 2× mini smoke。目标现象(SDC / SIGSEGV 类崩溃)= 0/29。**

- **全 shift 穷尽**:rot-s1..s15 + shift-0 族 7 运行(rot-s0-r3/r4、rot-s0-long 2h、rotate-s0-r1/r2、rot0-ref-m1/m2)= 16/16 shift;每 shift 是 rank↔die 全排列 → 无论哪个 rank 坐哪个 die(含 die3/NUMA3)全部 clean。
- **复现性**:shift-0 同配置 7 次重复(跨 2h 长跑 + 隔日 ref)零漂移。
- **Rule of Three(app 级终值)**:n=29 零事件 → 事件率上界 **10.3%**(95% CI);C2/C3 的 SIGKILL 已根因为 HBM 配对 OOM(§4.7),不计入。
- **执行完整性**(§4.9):core139 实际执行 759 采样/24 运行;NUMA3 覆盖运行 36-38/38 核活跃;全程 1550MHz。
- **内存落位全量审计**(2026-09-23):120 rank 快照 × 30 运行全部 1:1 正确(bind==die+16、零 HBM 共享),"内存落位错误"假说全量排除(diagnostics/numa-placement-audit.txt)。

| 其余章节 | 数据源 | 状态 |
|---|---|---|
| 5.2 mini 608 核扫描(Phase A 16 die × NT=36 + Phase B NUMA3 逐核 NT=1 + Phase C 对照) | job 1700367 | **全量收官(05:59):64/64 全清** |
| 5.3 mini 单因素裁剪(fd/grid/mix/NT/DDR/seed/check) | job 1700636 | PENDING |
| 5.4 mini 稳定性统计(die3 ×10 vs die0 ×10) | job 1700636 | PENDING |
| 5.5 stats20(n→32 阴性上界强化) | job 1700642 | PENDING |

### 5.2 mini-sweep Phase A:16 die 整域全清(2026-09-23 04:3x)

job 1700367 第 1/3 阶段收官。每 die 300s、NT=36(cores d*38..d*38+35,OMP close)、HBM membind d+16(die3→19)、v6 mini 二进制(sha256=42274d1b…)。

**结果:16/16 die 全部干净(rc=0, cls=ok),零 SDC、零崩溃、零 crash-note。**

| 指标 | 实测 |
|---|---|
| 吞吐范围 | 309.34(die4,全场最低)— 349.65(die8)it/s |
| 均值 ± 样本标准差 | 341.28 ± 9.77 it/s |
| die3(历史崩溃 die) | 331.44 it/s,z=-0.73,与其余 die 中位数之比 0.960 —— 带内,无性能异常 |
| checks | 5800-6556/300s,与迭代数同比例;hbm_mb=55 各 die 一致 |
| NUMA 落位 | mem_node=die+16 全部正确(audit_numa_placement.py 同判) |

判定(决策树,diagnostics/core-level-localization.md §4):die3 与其余 15 die 在同一 mini 载荷下无可分辨差异 —— **mini 载荷在 1550MHz userspace 钉频门控下不触发**,与 app 级 29 clean(§5.1)一致。诚实条款:Phase A 阴性 ≠ "die3 无缺陷";频率门控为历史复现的疑似必要条件(§4.10 统计对比、§4.11 机制取证),解除钉频后 mini-die03(NT=36)为首选复验点。

**Phase B 定稿(38/38,05:39)**:NUMA3 全 38 核(core114-151)NT=1 逐核 120s 全部干净——零 SDC、零崩溃,合计 **10,141 次黄金值校验全过**。吞吐(除首跑核冷效应)均值 35.80 ± 0.46 it/s:core139=35.62 正处均值;唯一偏低 core151=33.46(-6.5%,功能全过,吞吐观察);首跑 core114=29.77 为冷启动效应。

**Phase C 定稿(10/10,05:59)**:对照核(die0/2/4/6/8/10/12/14/15,双 socket)全清,吞吐 34.32-36.00 —— 与 NUMA3 Phase B 带宽完全重合,嫌疑域与对照域无可分辨差异。

**Task 6 结论(三粒度定稿)**:整节点 app(29 clean)/ 整 die(16/16)/ 单核(38+10)在 1550MHz userspace 钉频门控下全阴性;die3、core139 与对照无任何可测量差异(功能+吞吐)。cpu-results.csv 终版:608 行、608/608 覆盖、core139 累计 33 次运行 28696s 零失败、全表 failures=0。Rule of Three 合计:app 29 + sweep 64 = 93 次零事件 → 上界 3.2%。诚实条款:阴性 ≠ 无缺陷,门控解除后按 minimal-reproducer 包 + mini-die03/mini-core139 顺序复验。


### 5.3 Task 8.2 单因子消减:9/9 有效因子全清 + grid55 无效配置诚实记录(2026-09-23 06:44)

job 1700636(mini-phase2),每因子 300s,die3(cpus 114-149)NT=36、HBM node19。基准 red-base:grid 110x80x40 / fd=12 / Nd=352000 / mixh=6 / threads=36 / check-every=8 / seed=1,331.61 it/s(300s 内 99,484 迭代 + 6,217 次金标校验)。

| factor | 变化 | rc | it/s | delta% | checks | hbm_mb |
|---|---|---|---|---|---|---|
| red-fd6/fd4/fd2 | fd 12->6/4/2 | 0 | 437.45/472.90/525.49 | +31.9/+42.6/+58.5 | 8202/8867/9854 | 52.5/51.9/51.2 |
| red-mix1 | mixh 6->1 | 0 | 437.68 | +32.0 | 8206 | **26.8** |
| red-nt9 | threads 36->9 | 0 | 208.37 | -37.2 | 3907 | 55.0 |
| red-ddr | mem_node 19(HBM)->3(DDR) | 0 | 324.62 | **-2.1** | 6086 | 55.0 |
| red-seed2 | seed 1->2 | 0 | 331.87 | +0.1 | 6223 | 55.0 |
| red-check1 | check-every 8->1 | 0 | 46.18 | -86.1 | 13,854 | 55.0 |
| red-grid55 | grid->55,40,20 | **2** | - | - | - | - |

- **9/9 有效因子全部 clean**(rc=0、type=clean):门控下消减方向与全参数方向同样零触发,单因子层面无任何可测差异。
- **red-grid55 无效配置(诚实记录)**:mini_phase2.sh:55 误传 --grid 55,40,20,而 mini_laplacian 接口为 --nx/--ny/--nz;二进制 1 秒退出 rc=2(run-red-grid55.out = "unknown arg --grid",19 字节)。系本人脚本编写错误,grid 因子本轮未测;修正调用(--nx 55 --ny 40 --nz 20)已定义,待 die3 空闲(8.3 之后)补测。record_run 将 rc=2 标 cls=ok 为分类器缺陷(P0 扫描按 rc 键控未受影响),分析已覆盖为 invalid。
- **机理侧发现**:mem_node HBM->DDR 仅 -2.1% —— 工作集 Nd=352000x8B≈2.75MB 常驻 LLC,该规模计算受限,与 Phase B/C 各核吞吐带窄(35.80±0.46)互证;mixh 是足迹杠杆(55->26.8MB);check-every=8 是吞吐(every=1 时 -86.1%)与捕获延迟的折中;seed 无关(确定论负载)。
- **最小用例定义(结构保真)**:门控 0 复现,无法按复现力定最小;**最小用例 = red-base 原始参数**(即 minimal-reproducer 包默认)。缩减代价排序:check-every(机制本体)> threads > mixh > fd(机制成分)> grid(未测)> mem_node(近自由)> seed。解除钉频后先跑基准,复现则逐因子二分。详见 RES/minimization/reduction-results.md。

### 5.4 Task 8.3 稳定性统计:die3x10 vs die0x10 全清,无差异(2026-09-23 10:45)

mini-phase2(job 1700636)收尾系列,同 red-base 参数(grid 110x80x40/fd12/mixh6/NT36/check-every=8/seed1),每轮 300s,历史崩溃域与对照域各重复 10 次:

| 组 | n | 结果 | 均值±σ it/s | CV | min-max | checks/300s |
|---|---|---|---|---|---|---|
| die3(cpus114-149,HBM19) | 10 | 全 rc=0 clean | 332.89 ± 2.88 | 0.87% | 325.16-336.24 | 6096-6304 |
| die0 对照(cpus0-35,mem0) | 10 | 全 rc=0 clean | 334.32 ± 3.09 | 0.92% | 326.22-336.85 | 6116-6315 |

- **Welch t=-1.07(不显著),带宽完全重叠**:同参数重复下,历史崩溃域 die3 与对照 die0 在吞吐与正确性上均无任何可测差异;20 次运行零 SDC、零 crash,无首错时间。
- **Rule of Three 更新**:mini n=96 -> crash/SDC 上界 3.1%;合计独立运行 n=136 -> 任一目标现象率上界 **2.2%**(app 干净 n=40 -> 7.5%)。目标现象候选仍为 0。
- 该表双重作用:(a) 门控下 die3 无缺陷的又一独立证据;(b) 门控解除后复验时的同参数波动基线(±2.88 it/s 即噪声带)。
- 运维事件(诚实记录):07:49-10:41 登录节点 SSH 连接中断约 2.9h(22 端口超时,期间 8 次巡检无法执行);计算节点作业不受影响 —— phase2 按时于 ~08:30 结束,stats20 自动接续。恢复后一次性核对全部落盘记录。

### 5.5 stats20 收官 + matrix3 修复对照提交(2026-09-23 13:30)

- **stats20(job 1700642,12:25:52 结束)**:ppr-s0 r5-r14 + rot-s0 r3-r12 交错 20 跑(~720s/跑),连同更早 ppr r1-r4,共 28 条记录**全部 clean_iter5** —— 每跑均到达 iter5,远超历史崩溃窗口(iter1-2),零 SDC 零 crash。高频崩溃配置(ppr/rot shift0)在钉频门控下的阴性统计加强完成。
- **Rule of Three 终值**:app 干净 n=49 → 上界 6.1%;mini n=96 → 3.1%;**合计独立运行 n=145 → 任一目标现象率上界 2.1%**。目标现象候选 0(全程)。
- **matrix3_fixes 已提交:jobid 1706130**(-T 7200,预期 ~1.5h):C4-oomrepro(原 C2 配对 rankfile + 密集 numa_maps 采样,直接抓 HBM 池增长至 4GB 后 SIGKILL 的机制证据)+ C3fix-nt19-11(np=16,1 rank/域,NUMA3 由 rank3 承载)+ C2fix-nt19-n3idle(np=15 避开 NUMA3)—— 后两者验证 nt=19 本身无问题,证实 C2/C3 崩溃纯属 HBM 配对 OOM。
- grid55 补测(--nx 55 --ny 40 --nz 20)安排在 matrix3 结束后的节点空闲窗口(matrix3 的 rankfile 含 NUMA3,期间不注入避免污染)。
## 6. 结论(待最终数据)

- 工作假设:H1(硬件)— 待 608 核扫描与逐核数据检验;若全阴,则结论转向"1.55GHz 环境下不复现,与历史环境的差异(频率/固件/负载混合)为开放问题",并给出统计上界。

## 7. 复现指引

- app 级:`RES/scripts/baseline_series.sh` 系(matrix_common.sh 封装),BIND_MODE/NUMA_SHIFT 参数化。
- mini 级:`RES/minimization/run_mini.sh`(单跑)/`mini_sweep.sh`(608 核)/`mini_phase2.sh`(裁剪+统计);编译配方与退出码见 `RES/minimization/README.md`。
- 判定标准:SDC = 正常完成但 r≠golden(退出码 3,含 elem/addr/golden/actual/core 落盘);崩溃 = 信号(退出码 4 + crash-note);二者严格区分。

### 4.7 C2/C3 SIGKILL 根因:HBM 配对 OOM(对照设计缺陷,2026-09-23)

matrix1 收尾的 C2/C3 两条 outcome=crash 深挖结果:被杀 rank(C2:rank15/NUMA8、C3:rank1/NUMA0)均为 **SIGKILL**,SCF init 后 ~35s,与目标现象(SIGSEGV/NUMA3/iter2-3)形态完全不符。机制:nt=19 rankfile 每域 2 rank → 两 rank 的 hbw 池(memkind HBWM_POLICY_BIND;C1 快照实测 rank0→bind:16)同绑一个 4GB HBM node(MemTotal 实测 4GB)→ 2×3.75GB(源码固定常量)预留 > 4GB → touch 时内核 OOM SIGKILL。旁证:22+ 次 1:1 配置(nt=35/36/38)每 rank HBM touched 恒 3.0GB 全 clean。**排除出目标现象统计**(analyze_queue.py 已加 oom_hbm_pairing 类);修复对照(matrix3_fixes.sh:C4 机制复现 + C3fix/C2fix)已备、stats20 后提交。教训:改 NT 的对照必须保持 1 rank/域(hbw 池按域绑定)。

### 4.8 v6 崩溃取证升级 + 遥测(Task 7 收尾)

mini_laplacian v6:崩溃处理器改为落盘完整 ucontext(FAR/pc/sp/pstate/x0-x30)后 signal(SIG_DFL)+return → systemd-coredump 捕获原始寄存器,退出码 128+sig。已验证:handler_test rc=139、core 解析 code=1(SEGV_MAPERR 真实内核 fault)、pc 与 note 一致;干净路径冒烟(2939 迭代/checks=183)与 v5 一致。遥测(dattach 实测):perf_event_paranoid=2(用户态计数可用);EDAC=/sys/devices/mc0/,负载中 ce=0 ue=0 —— campaign 全程零 ECC 事件,与 DRAM 位翻转假说不符。Task 7(微架构诊断)5/5 完成,证据落盘 RES/diagnostics/(historical-crash-microarch-analysis.md、node-coredump-inventory.md、c2c3-sigkill-root-cause.md、diagnosis-report.md)。
### 4.9 执行证据闭环 + 频率全程定量(2026-09-23 02:12)

针对"0 复现是否因为目标核根本没被压到"的质疑,scripts/verify_exercised.py 解析 campaign 全部 28 个运行的运行期采样日志(run-*.threads.txt 的 psr 字段、run-*.freq.txt 每 10s 三核频率):

- **core139 实际执行**:24 个运行中 cpu139 承载过执行线程,campaign 累计 759 个 psr=139 采样点(每运行约 22 个采样 × 10s 间隔;rot-s0-long 2h 长跑独占 230 个)。"0 复现"结论由此闭环——目标核被反复真实压载,非空转。
- **NUMA3 核活跃度**:所有 NUMA3 覆盖运行(rot×16/ppr×6/C1/C3/FULL608)实测 36-38/38 核执行(标准运行 rank 占 114-149,150/151 留空;FULL608/C3 为 38/38);C2(numa3idle 对照)正确 0 覆盖——对照组自身有效。
- **频率定量证伪**:28/28 运行 × cpu139/cpu0/cpu304 全程 1550MHz(rot-s0-long 1549-1550,1MHz 抖动)。用户所述"cn23154 已恢复 2GHz"与全程实测不符。**推论**:若目标现象频率相关,本 campaign 工况系统性偏离历史工况——这是 0 复现的头号候选解释,但未被证实(无 2GHz 工况对照数据)。证据:RES/diagnostics/exercised-coverage-proof.txt。
### 4.10 历史 vs campaign 统计对比:0 复现不是运气(2026-09-23 03:2x,决定性)

材料 logs/ 的历史暴露普查:4 个真实 SCF 运行(ppr.shift0、rotate.shift0/1/2)**4/4 全部 signal 11 崩溃**——全部是 die3 上的 rank(shift 映射逐一验证),全部在 iter1-2 早期;ppr.shift0.jzptest 仅 446 行、止于 OMP env 显示,**未进 SCF 载荷**(非暴露,不计分母)。本 campaign 29 个 app 运行(同二进制 binary_sha256=804792f3…==materials a.out、同输入、同 36 核/die 落位)**全部到达 iter5**——远超历史崩溃窗口——0 崩溃 0 SDC。P(0/29|p=100%)<1e-20(按 p=50% 极端保守也 1.9e-9)。**结论:环境门控在 09-17(历史日志 mtime)至 09-22 之间改变,0 复现是环境变化的确定性结果而非采样不足。** 唯一实测到的环境差异 = 1550MHz 锁频(历史日志无频率数据,PULAY_FREQUENCY 是 SCF 参数的假阳性;"2GHz 已恢复"与 campaign 全部实测矛盾)。频率门控为最强未证假设;可检验预言:恢复 2GHz 应恢复崩溃(需运维,超出本 campaign 权限)。详见 RES/diagnostics/historical-vs-campaign-statistics.md。
### 4.11 频率门控机制取证:userspace 钉 1550MHz,2GHz 开放但未选(2026-09-23 03:5x)

dattach 只读 sysfs 采集(job 1700367):全节点 **scaling_governor=userspace,scaling_setspeed 钉 1549-1550MHz**(12 个采样 CPU 覆盖 16 die × 2 socket,值域 1549029-1550001 kHz);同时 scaling_max_freq = cpuinfo_max_freq = **2000000 kHz(2.0GHz 上限开放)**,scaling_min_freq=1200000。即:**2GHz 硬件可达且策略上限开放,但 userspace 钉值把实际工作点压在 1550MHz**——这是管理性限频(非热 throttling、非硬件钳制),与 campaign 全程 1550MHz 实测(scaling_cur_freq 采样)在机制层闭环。"已恢复 2GHz"的说法与实际工作点不符(最宽容的解读:max 上限恢复了,但 setspeed 钉值未动)。频率门控假说的可检验预言(恢复 2GHz → 崩溃应重现)只能由运维执行——写 scaling_setspeed 是本 campaign 明令禁止项。证据:RES/environment/freq-policy-snapshot.txt。

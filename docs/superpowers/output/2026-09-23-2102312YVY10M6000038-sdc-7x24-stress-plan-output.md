# SDC 7×24 压测战役结果（TaiShan 2280 / SN 2102312YVY10M6000038）

> 对应方案：`docs/superpowers/plans/2026-09-23-2102312YVY10M6000038-sdc-7x24-stress-plan.md`
> 本文档只记录**实测**：每条结论附命令输出或日志依据。战役进行中持续追加。
> 时间说明：本机 NTP 不可用（UDP/123 网络层阻断，已实测并回滚），时间戳为系统自由运行时钟，事件台账与 BMC 时间双源互证。

## 1. 执行时间线（Phase 0–5）

| 时间 | 事件 | 依据 |
|---|---|---|
| 2026-09-23 18:49 | Phase 0 就绪检查完成：裸金属 ✅（`systemd-detect-virt`=none）、磁盘 ✅（/home 124G）、网络 ✅、NTP ❌（chronyd 全源 reach=0）、**非测试专用机**（llama-server 16t/qemu 4vCPU/TDengine/piper 常驻——用户决策：全部保留共存压测） | driver 会话记录 |
| 18:53–19:22 | Phase 1 全量画像采集（16 文件归档 inventory/） | `docs/superpowers/inventory/2102312YVY10M6000038-2026-09-23/` |
| 19:23 | 方案审批（4 项决策：背景服务保留/root 监控服务/NTP 尝试修复/批准执行） | 会话记录 |
| 21:10–21:15 | Phase 2 构建完成 | §2 构建记录 |
| 21:24 | NTP 修复失败定论（三源 iburst+burst 均 reach=0，chrony.conf 已回滚） | `chronyc sources` 输出 |
| 21:26–23:57 | 战役工程实现（6 个 commit）+ 两轮 smoke 演练 | §3–§4 |
| 23:19–(进行中) | smoke 第二轮验证 | §4 |

## 2. 构建记录（Phase 2）

**vendored 依赖**（openssl/openblas/sleef 为既有构建，本日新增）：

```
isa-l:   libisal.a 451,960 B（build.sh rc=0）
acl:     libarm_compute-core.a 18,418,758 B（ACL_BUILD_RC=0，arm_compute_core 目标）
meson:   setup 成功（buildtype=release）
ninja:   381/381 无 error → builddir/sdcshield（271MB）
```

**L0 冒烟**：

```
--list-tests = 329 个用例（≥README 278 基准；含 isal_*/acl_gemm/eigen 全家）
zstd19 -n 1 -t 2000 → rc=0，4 × result: pass
--dump-cpu-info → family 48-d01 "firestorm"（Kunpeng 920 内部名），特性与画像一致，无 SVE
```

**参数复核矩阵**（--help 文本不完整，以源码选项表 + 解析探测 `$BIN <flag> -l`（rc=64=不存在）为权威）：

| 旗标 | 结论 |
|---|---|
| `-T --strict-runtime --cpuset -Y -o --disable --ignore-timeout --ignore-os-errors(单复数均可) --max-test-loop-count --quality --on-crash --on-hang --rng-state --test-list-randomize --test-delay --retest-on-failure` | **全部存在**（实测可解析） |
| `--vary-frequency --vary-uncore-frequency` | **不在本构建**（di/dt 走 governor 代理） |
| `--temperature-threshold` | 存在但**本板 no-op**（`thermal_monitor.hpp` 已知热区类型表不含 `acpitz`；threshold=1000 实测正常跑完）→ 热保护由 root 监控服务唯一承担 |
| 未知选项 | rc=64 `unrecognized option` |
| YAML 种子行 | `state: { seed: 'AES:<64hex>', iteration: N, retry: bool }` |
| 失败信号 | `result: fail` / `result: crash`；崩溃 rc=1（selftest_sigsegv 实测：crashed:true, core-dump:true, RIP/FAR 上下文落 YAML，需 -vv） |

**关键语义实测**（驱动设计的依据）：

| 实验 | 结果 | 结论 |
|---|---|---|
| `-e` 含不存在用例名 | 瞬退 `Cannot find matching tests` | 驱动所有 `-e` 经 resolve() 预过滤 |
| `-T 4s -t 3s` 三用例 | 9.11s | 有限 `-T` 单独**不硬停** |
| 同上 + `--strict-runtime` | 6.08s（2 用例完整） | ≈预算+在飞用例，采用 |
| `-T forever` + SIGTERM | 60s+ 不退出（实测） | L3 用自终止预算，弃外部 kill |
| `-q` | 种子行 0 条（默认 4462 条） | **弃用**（取证必需） |
| `--max-messages=50` | 体量几乎不减 | result 行豁免，无效 |
| YAML gzip | 2.15MB→178KB（12×） | 每日压缩方案 |

**governor 可用性**：`conservative ondemand userspace powersave performance schedutil`（cppc_cpufreq，performance 恒 2.6GHz）——di/dt 阶跃成立。

## 3. 战役工程实现（commit 清单）

| commit | 内容 |
|---|---|
| 28cf32c1 | docs: Phase0/1 画像 + 7×24 plan + 指令文档 |
| 8c228b9c | feat: sdc_common.sh + collect_inventory.sh（幂等验证：两轮文件清单一致、无凭据落盘） |
| 5c7ca597 | feat: sdc_monitor.sh（双格式 ipmitool 解析实测修正；root 建目录归还属主；alerts.log 666） |
| 9dcddada | feat: sdc_campaign.sh（L0–L5 驱动 + 断点 + 取证） |
| a2447590 | feat: systemd units + logrotate + install/start/stop/status |
| fa67f831 | docs: NEW_BOARD_ONBOARDING.md + README 模式 E |
| 8049c712 | fix: SELinux ExecStart（AVC 实测）+ run_bounded（-t 每用例语义实测） |
| 975e455b | fix: smoke 第二轮（kill artifact 判别/L3 自终止/复测封顶/每日 gzip/SWEEP=4m） |

## 4. smoke 演练（两轮）

### 第一轮（22:10–23:15，发现 4 个真实缺陷）

| 验证项 | 结果 |
|---|---|
| 冷机首轮（45 用例 resolve 解析） | ⚠️ 暴露 `-t` 每用例语义 → 修复为 run_bounded |
| L2 spectrum 1a–1f + dwell | ✅ 全部 `exit: pass`（17 个 YAML） |
| ipsec datasize 扩展档 | ✅ 16MB 档按设计被 timeout 兜底正常切换 |
| monitor 服务 | ⚠️ SELinux AVC（init_t 执行 user_home_t 被拒，203/EXEC 循环）→ ExecStart 改 /bin/bash 后 active |
| governor 代理 | ✅ request→done 回执、powersave 时主频 200MHz、恢复 2600MHz、延迟 ≤70s |
| check_pause 联锁 | ✅ PAUSE 存在阻塞（timeout 终止）、删除即放行 |
| 事件取证写路径 | ✅ events/ 目录 + ledger.csv（注入演练） |
| L3 fixed | ❌ TERM 无响应 → KILL(137) → 被误判为事件 → 无界复测卡死 → 修复（见第二轮） |

### 第二轮（23:19–00:16，全部修复后的回归验证，**全链路 PASS**）

| 验证项 | 结果 | 证据 |
|---|---|---|
| 冷机首轮 | ✅ 2m46s（预算 2m + 在飞用例） | driver.log 23:19:49→23:22:35 |
| L2 spectrum 1a–1f + dwell | ✅ 全 pass | logs/spectrum_c1_files/ |
| L2 ipsec 扩展 | ✅ 3 档完成；16MB 档兜底 kill **被正确分类为非事件** | driver.log 23:45:41 "被阶段边界终止（rc=124，无 fail/crash 行）——非事件" |
| L3 固定序/随机序/eigen | ✅ **-T+strict 自终止**（fixed 3m18s / rand / eigen n1），无外部 kill | 234715/235033/235137 YAML |
| L5 热激发（cycle1 轮到） | ✅ 预热 5m + 目标档，末态 69/48°C | 235302/235830 |
| L4 深驻留 | ✅ 5 对象 × 60s（dgemm/sleef/pocketfft/isal/openssl_sha），`--max-test-loop-count=0` | logs/20260924/001024–001525 |
| 日汇总 + gzip + 退出 | ✅ "smoke 完成（全链路走通）"，进程干净退出 | daily_summary.log |
| 事件台账 | ✅ 零真实事件、零伪事件（fail/crash 文件数 0） | daily_summary |
| 监控服务独立性 | ✅ 全程 active，CSV 60s 一行，bmc_ok=1 | monitor.csv |

两轮 smoke 共暴露并修复 **8 个真实缺陷**（SELinux exec / -t 每用例语义 / TERM 无响应 / kill-artifact 误判 / 无界复测 / 日志体量 / SWEEP 预算 / governor 代理时序），全部以实测证据修复并回归。

## 5. 事件台账

| 时间 | label | rc | test | seed | 复测 | 定性 |
|---|---|---|---|---|---|---|
| 2026-09-23 22:19 | injected-drill | 1 | — | — | — | **演练注入**（取证写路径验证，非真实事件） |
| 2026-09-23 23:17 | l3_fixed_c1 | 137 | — | — | — | smoke1 工程伪事件（驱动兜底 kill 误判，已修复 975e455b） |
| **2026-09-24 08:26** | **l3_fixed_c1** | **137** | **mesh_upi_sse_asymm_distrib_int** | **AES:ebcafcfb…** | 整轮×3=0失败；定向全核 3×120s+600s=0；逐域×4=0 | **事件 #1：条件触发/间歇性 SDC 类候选**（详见下） |

### 事件 #1 详情（首个真实 SDC 类事件 → **subagent RCA 定案：测试内部竞态，伪 SDC**）

> **2026-09-24 10:30 修订**：按用户指令派 subagent 做源码级根因研究后定案——本事件为**测试 bug 造成的伪失败，非硬件缺陷**（置信 ~95%）。初版"条件触发/不可复现"结论部分无效：定向"全核"复现命令因 `-n 0` 被框架钳位为 1 线程（单写者无读者，空转通过）。
>
> - **根因**：`mesh_upi_sse_asymm_distrib_int.cpp` L195-201 读者自注册轮次屏障"快照欠计+非原子重置"竞态——首读者见 rc=1 独自重置轮次，被抢占期间迟到读者校验跨轮混合数据，被写者覆写 → sum 失配。6 个失败实例全部 `consistent=1`（本地数据从未错 = 无数据损坏）、受害者均死于首次校验（注册窗口内）
> - **稳定复现**：`-n 8 -t 10s` 命中 4/6；node0/node3 均中（与 NUMA 无关）；种子无关；`-n 2` 永不失败（结构对照）
> - **同族带病**：`mesh_upi_avx2_asymm_distrib_int`（同屏障 bug 未修复）；`avx2_asymm_int` 已修的屏障（thread_count()+sense-reversal）从未移植到 distrib 变体
> - **处置（已闭环，用户决策：subagent 修复、不隔离）**：屏障修复补丁 `71b0a9cc`（两个 distrib 变体移植 `thread_count()` + arrived/epoch sense-reversal 屏障，写者参与，校验逻辑零改动）。验证矩阵：修前 sse/avx2 均 2/2 失败 → 修后两测试 ×（8×10s + 2×60s + `-f no`×3 + `-n 2`）**0 失败**、迭代画像与修前一致（非空转）、回归 zstd19 pass、0 新警告；主 agent 独立复验 2×0 失败。**战役二进制已 ninja 重建并实测 3×0 失败**——驱动每次调用重新 exec，cycle 2 L3 起自动使用修复版，测试无隔离恢复在列。`sse_asymm_write_int` 审计结论：无同款竞态（死字段 + 单读者轮转时序健全），未修改
> - **遗留（独立单元待办）**：switching-write-core 覆盖缺口（描述承诺切换写核，实现固定 id==0；`my_epoch % total_threads` 范式现成）；`sse_asymm_write_int` 自旋节流审计；框架对 inter-core 类测试 `-n 0/1` 的告警建议
> - **框架级发现**：`-n 0` 静默钳位为 1 线程（与 --help 语义"=CPU 数"不符）——已记录，建议框架侧改 warning/error


- **失败**：`mesh_upi_sse_asymm_distrib_int`（1 写核→127 分布式读核一致性，NEON int32 L1/L2，切换写核）
  - 07:14 iteration5：logical **CPU 66**（NUMA2, module 8544），ttf 2.51s
  - 07:15 框架自动重试：logical **CPU 85**（NUMA2, module 11124），ttf 8.16s（同 seed）
  - 框架内建重试序列：fail → pass → fail
- **工况**：温度 65-76°C（**与 08:21 热浪无关**——时间戳为 UTC 换算后确认；08:26 事件目录时间 = 热联锁 KILL 触发 YAML 扫描的时刻）；功率 ~300W；EDAC/rasdaemon 全清白
- **复现尝试（全部 0 失败）**：整轮重放 ×3（900s，证据弱——轮转前段未达 mesh）；定向全核 3×120s + 600s；逐 NUMA 域 4×60s（本板 --cpuset 仅支持逗号逻辑 CPU 号——pN 语法因 PPTT 伪影 package ID 36/8442 匹配 0 核，修复 0c279167）
- **分类**：条件触发/间歇性（需要轮转上下文的特定前置状态，契合文献 ITHICA execution-context 现象）
- **候选解释**：(a) NUMA2 互联/一致性边际缺陷（SDC 类，战役目标）；(b) 测试内部同步竞态（mesh 家族 2026-08 reader-starvation 修复后的残余风险）
- **判决路径**：今晚 L4 以 mesh 为第一驻留对象（campaign.env 已置）+ 7 天持续暴露；复现聚 NUMA2 → 硬件方向；不再现 → 倾向测试竞态
- **驱动缺陷随事件修复**（c2333e2c/0c279167）：提取式取证（原 2.76GB 整文件复制）、失败测试/种子从 .out 摘要提取（原错取轮转首用例）、定向复测替代整轮重放、逐域 cpuset 语法
- **证据**：`events/20260924-082650-l3_fixed_c1-rc137/`（yaml_extract、targeted_forensics/、FORENSICS_SUMMARY.md、root 快照 20260924-082757）

### 热事件（非 SDC，运维记录）

- **08:08–08:27 满核重负载热浪**：CPU1 66→**106°C（=Tjmax+1）**，固件节流以 3-10s 周期振荡（SEL "Processor State" 60+ 事件，08:10 起）；60s 采样首次检出 95 时实际已 105；旧 5 分钟升级窗口使 105-106 持续 ~5 分钟后 KILL（08:26:49），1 分钟回落 30°C。EDAC/rasdaemon 清白 = 纯热事件。**Prochot 传感器全程 0x00（本板无用，节流只见于 SEL）**
- 联锁收紧（158e5d7f）：≥88°C 时 20s 快采样、绝对线 100°C 立即杀、连续 2 热采样升级杀、连带杀 stress-ng
- cycle 1 的 l3_fixed 因热杀与取证停车提前结束（2h13m/6h 预算）；cycle 2 于 09:53 重新起算

## 6. 战役运行统计（持续更新）

| 指标 | 值（截至 2026-09-24 13:22） |
|---|---|
| 正式启动时间 | **2026-09-24 00:20:15**（sdc-campaign.service active，cycle 1 冷机首轮起） |
| 目标时长 | ≥168h（7×24），可延长 |
| 累计运行 | ~13h 墙钟（其中 09:16–09:50 受控停车取证 35min，有效压测 ~12.4h）；完成 24h 周期 1（cycle 2 进行中，L2 谱系扫档） |
| 覆盖维度 | cycle1：冷机代表集 45 用例 + L2 全谱（GEMM 尺寸/形态、SLEEF 足迹、FFT 因子、压缩 level、crypto/LU、ipsec 数据 3 档、memcpy 三策略）+ L3 全用例轮转 2h13m（热终止）；cycle2 至今：冷机 + L2 进行中；全程 stress-ng --verify 逐时轮转 + rasdaemon + 60s IPMI/OS 时序（775 样本） |
| 温度峰值（CPU1/CPU2） | **106°C / 71°C**（08:22 满核热浪，联锁收紧后未再超 95）；当前 79/55°C |
| 功耗峰值 | 474W |
| Prochot 传感器 | 全程 0x00（本板无信号，节流只见 SEL） |
| EDAC ce/ue 增量 | 0/0；rasdaemon 无任何错误记录 |
| 热联锁动作 | PAUSE 5 次、热升级 KILL 2 次（均在旧联锁时期 00:13/08:26；收紧后 0 次）；KILL 后 1 分钟内回落 ≥30°C |
| 事件净值 | **真实 SDC/硬件事件 0**（事件 #1 定案为测试竞态伪 SDC，已修复闭环 `71b0a9cc`） |
| 产出日志 | 73 个 YAML（1.1G，每日 gzip）+ 完整 monitor.csv 时序 |
| 工程提交 | 19 个 commit（画像/方案/脚本/systemd/两轮 smoke 修复/事件取证与 mesh 修复） |

## 7. 结论与建议（战役结束后定稿）

- （进行中）"未检出"≠"无缺陷"：置信度取决于覆盖维度 × 累计时长，最终按 plan §5.2 口径给出。

## 附：缺口与决策记录

1. NTP 不可用（UDP/123 阻断，实测）——双源时间戳互证。
2. `--temperature-threshold` 本板 no-op——root 监控为唯一热联锁（60s 周期 + 滞回 95/90°C）。
3. 背景服务共存（用户决策）——逐核隔离结果带噪声注记，事件定性核对 monitor.csv 负载列。
4. FAN1/4 无读数、PS1 缺失——联锁只覆盖 FAN2/3 + PS2。
5. 研究引用以仓库 31 篇综合报告为准（外网搜索本环境不可用，如实声明）。

## 附2：磁盘清理操作记录（2026-09-25，sdc-excite-reproduce M0 Task 1）

**背景**：/home 用量 86% 超过 v5 §5.6 磁盘联锁 85% 告警线，M0 运维前置任务要求降至 <85%。删除清单经用户逐项批准后执行；本记录引用的全部数字为实测输出。

### 清理前（2026-09-24 分析时实测）

```
$ df -h /home
文件系统                    大小  已用  可用 已用% 挂载点
/dev/mapper/opneuler-home 1013G  824G  138G   86% /home
$ df -k /home
/dev/mapper/openeuler-home 1062006752 863668336 144317708  86% /home
```

真实比率 85.68%（df 口径 used/(used+avail)）。

**外部变化（非本任务操作，如实记录）**：2026-09-25 09:18 前后，`vmcore0102/127.0.0.1-2026-08-24-18:03:07/` 下的 gzip 压缩分卷（*.gz.part.*，合计 71.4G）被本会话之外删除（原始 vmcore 109G 保留）。此后 df 降至 79%（used 790578176 KB，真实 78.43%）。

**反事实与归因（关键，防未来误读）**：本任务批准项合计仅释放 5530480 KB（5.27GiB）。若无上述外部删除，清理后 used 将停在 858137856 KB（= 863668336 − 5530480），真实比率 858137856/1007986044 = **85.13%**，df 显示 85%——**仅凭批准项（A1-A8+B12+B10）不达标，无法跨过 85% 线**。本次 86%→78% 的达标是**外部 71.4G 分卷删除 + 本任务 5.27G 清理的共同结果**，不应归功于批准清单本身；未来若再次逼近 85% 告警线，重复同类清理项不足以达标，需另行裁决 B 类大项（vmcore 原件 ~440G、gem5 输出 82G+24G、containers 29G 等，本次均明确未删）。

### 用户批准并执行的删除项（逐项实测释放量）

| 项 | 路径 | 释放量（df avail 差） |
|---|---|---|
| A1 | `sdcshield/builddir/` | 870384 KB（850M） |
| A2 | `third-party/openblas/OpenBLAS-0.3.29/` | 268732 KB（262M） |
| A3 | `third-party/openssl/openssl-3.5.0/` | 193680 KB（189M） |
| A4+A5 | `third-party/acl/ComputeLibrary-23.02/` + `third-party/acl/build/` | 117336 KB（115M） |
| A6+A7 | `third-party/sleef/build/` + `third-party/sleef/sleef-3.9.0/` | 30116 KB（29M） |
| A8 | `third-party/isa-l/isa-l-2.32.1/` | （并入上行动作，du 实测 9M） |
| B12 | `.claude/worktrees/memcpy-rewr-default`（`git worktree remove`，分支 feat/memcpy-rewr-default-mode 已合并进 main，保留于 git） | 1006808 KB（983M） |
| B10 | `/home/sdc/.cache`（bazel 2.4G + go-build 380M + pip 68M 等） | 3043424 KB（2972M） |

合计释放 **5530480 KB（5.27GiB）**，与分析期 du 预估（~5.4G）一致。明确未删：vmcore 原件、gem5 输出、containers、rpms、silifuzz、go、战役数据（`/home/sdc/tmp/sdc-campaign` 3.5G 完整保留）及一切 C 类。

### 清理后（2026-09-25 实测）

```
$ df -h /home
文件系统                    大小  已用  可用 已用% 挂载点
/dev/mapper/openeuler-home 1013G  749G  213G   78% /home
$ df -k /home
/dev/mapper/openeuler-home 1062006752 785047696 222938348   78% /home
```

真实比率 785047696/1007986044 = **77.88%，df 显示 78% < 85%，达标**。

### 验证与重建方式

- `git status --short` 全程干净（所有删除项均为 gitignore/未跟踪的构建产物；`compile_commands.json` 为 tracked 符号链接指向 builddir，暂成断链，`meson setup builddir` 后自愈）。
- builddir 重建：`PKG_CONFIG_PATH=./third-party/eigen5 meson setup builddir --buildtype=release && ninja -C builddir`。
- third-party 重建：各库目录保留 `build.sh + *.tar.gz + install/` 三元组，重跑对应 `third-party/<lib>/build.sh` 自动从 tarball 重解压重建（脚本含 install 短路保护，install/ 已在时不会重复构建）。
- C 类完好性复核：`/home/sdc/tmp/sdc-campaign` 3.5G、`sdcshield/.git` 3.5G、`docs/` 77M 均在；sdc-campaign/sdc-monitor 服务保持 inactive。

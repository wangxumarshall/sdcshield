# 【122 号核心候选】阶段 2 出现过 fail / 可疑信号的测试记录（2026-09-21 建立）

> **用户指示（2026-09-21）**：SDC 的出现有**偶然性**，阶段 2 的短窗口（30-60s）没复现 fail ≠ 负载不能触发 122。**先记录所有可疑信号，等全部 64 个转换写完后统一做长时间检测**。
>
> 记录规则：
> 1. 阶段 2 出现过任何 fail（复现或未复现）的测试 → **候选名单**
> 2. fail 有 cpu-mask 证据指向 122 的 → **高优先候选**
> 3. 记录必须保留：时间、轮次、pass/fail 计数、fail 的测试名、（如有）cpu-mask
> 4. 全部批次完成后：对候选名单做长时窗检测（小时级），验证 122 触发条件

---

## 候选名单

### 候选 #1：批次 2 全组（13 个测试联合运行）——1 次未复现 fail

- **时间**：2026-09-21 阶段 2 首轮（全核含 122，30s 窗口）
- **现象**：29755 pass / **1 fail**，exit: fail
- **归因证据**：⚠️ **丢失**——我在删日志前未提取 fail 的测试名和 cpu-mask（操作失误，已记入 commit 8f656917 与进度日志）
- **复现尝试**：4 轮复跑（约 10 万个测试结果）0 fail；122 每轮都参与运行
- **涉及测试**（该轮运行的 13 个）：neon_add_sve, fma_sve, fpu_special_values_sve, power_virus_dit_sve, movdq2q_sve, movq2dq_sve, movmskpspd_sve, fsu_byteexact_sve, kreg1_sve, kreg4_sve, kreg7_sve, swizzle_sve, insert_extract_sve
- **判定**：无法排除是 122 的低概率触发（也可能是我代码的偶发 bug 或框架噪音）。
  - 支持是 122 的理由：阶段 1（排除 122）同负载 30882/0 零 fail；fail 只出现在含 122 的全核轮
  - 支持不是 122 的理由：4 轮复跑 0 fail（但 SDC 偶然性下这不构成否定）
- **待办**：长时窗检测时**这 13 个整组优先**，且必须保留完整日志 + fail 的 cpu-mask

---

## 长时窗检测计划（全部批次完成后执行）

1. **候选组重测**：对候选名单整组 + 逐个，跑 1-4 小时时窗（`-t 3600000` 级），全核含 122
2. **证据保全**：fail 出现时立即提取 测试名 / cpu-mask / data-miscompare 详情（教训固化：先归因后清理）
3. **交叉验证**：同负载同窗口在排除 122 的 126 核上跑对照轮——若候选组在全核 fail 而排除 122 后零 fail，即锁定 122
4. **触发条件分析**：一旦 122 被长时窗捕获，记录触发的负载类型/时窗长度/复现概率，形成"什么负载能快速触发 122"的结论（任务纲领的最终目标）

## 历史阶段 2 记录汇总（供长时窗优先级参考）

| 批次 | 负载类型 | 阶段1（!122） | 阶段2（含122） | 122 是否参与 | fail |
|---|---|---|---|---|---|
| avx53-FMA（10） | 短向量 SVE FMA | 5488/0 | 6219/0 | ✓ | 无 |
| avx53-Mesh A（12） | 多核互联协同 | 175662/0 | 174146/0 | ✓ | 无 |
| avx53-Mesh B（6） | 4MB 内存带宽 | （并入 18 个统计） | 168516/0 | ✓ | 无 |
| avx53-eigen（1） | SVD 式密集 FMA | 5387/0 | 5411/0 | ✓ | 无 |
| avx53-ipsec（24） | EVP+多流 SVE HMAC | 3645/0 | 3701/0 | ✓ | 无 |
| 批次1-kreg（7） | 谓词指令 | 7954/0 | 7359/0 | ✓ | 无 |
| **批次2-NEON直换（13）** | **混合向量/谓词** | **30882/0** | **首轮 29755/1，4 轮复跑 0** | ✓ | **⚠️ 1 次未归因** |

> 注：批次 2 首轮的那个 fail 是全部历史运行中**唯一**在全核轮出现的 fail，且同负载排除 122 时零 fail——它是目前 122 候选名单的头号线索。

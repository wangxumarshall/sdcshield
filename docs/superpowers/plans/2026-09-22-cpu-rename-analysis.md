# CPU 编号变更分析（2026-09-22，应用户质询核查）

## 用户判断：完全正确 ✓

## 证据链（全部实测）

### 1. 两次启动对比
| | panic 期启动（9-17 11:39/11:43/11:46 三连崩溃） | 当前启动（9-17 20:08 至今） |
|---|---|---|
| CPU 总数 | **128**（`smp: Brought up 4 nodes, 128 CPUs`，docs/boot panic.txt） | **127**（`smp: Brought up 4 nodes, 127 CPUs`，journalctl -b 0） |
| nr_cpu_ids | 128 | 127 |
| wtmp | 11:39/11:43/11:46 三次重启（崩溃循环） | 20:08 一次启动至今稳定 |

### 2. panic 的确发生在旧编号 CPU122 上
docs/boot panic.txt：
- `CPU122: Booted secondary processor 0x0900060200`（旧122 = MPIDR **0x0900060200**，GICR 900060200）
- `GICv3: CPU122: found redistributor 900060200`
- Oops: `Internal error: Oops: 0000000086000004` @ 33.7s → Kernel panic

### 3. 旧 122 物理核已被固件屏蔽
- 本次启动 journalctl 中 **MPIDR `0x0900060200` 零出现**（不在 SRAT/MADT，OS 层不可见）——固件（BIOS）层屏蔽
- core_id 序列实证缺口：cpu121=core 290 → **cpu122=core 294**（292/293 所在物理核缺席，292 即旧122）

### 4. 编号整体下移：新 122 = 旧 123
- 新 cpu122 = MPIDR **0x0900060300**（journalctl: `CPU122: Booted secondary processor 0x0900060300`）= 旧 123 号物理核
- cpu122 拓扑：package 1, node 3, core_id 294（与 panic 期旧 123 的位置一致）
- 新 0-121 编号未变（前 122 个核连续）；123-126 各前移一位

## 对本任务（122 SDC 狩猎）的影响判定

**狩猎目标自始至终一致，证据有效**：
- 我们的 SDC 测试任务从 **9-19** 开始，全部运行在**当前这次 9-17 20:08 启动实例**内（无重启）
- 因此任务全程的 "cpu122" = **新 122 = MPIDR 0x0900060300**（core_id 294）
- 用户原始情报 "CPU 122 存在 SDC 故障" 如指当前编号体系，则我们压测的就是正确的核
- **待用户确认**：原始 SDC 情报中的 "122" 是指哪个编号体系？
  - 若指 9-17 20:08 之后（127 核体系）的 122 → 我们的目标正确（当前战役无需改变）
  - 若指 9-17 20:08 之前（128 核体系）的旧 122 → 那个核已被固件屏蔽，OS 无法访问，且它引发的是启动 panic（不是 SDC）——若 SDC 核是旧编号 122，则真正目标应是新编号体系中的 **123 号核（=旧124）或需重新定位**

## 规则更新（跨启动引用 CPU 编号必须带 MPIDR）

今后任何跨启动/跨会话的 CPU 编号引用必须以 **MPIDR** 为准；每次重启后须重新 dump `journalctl -k | grep "CPU122: Booted"` 核对编号漂移。

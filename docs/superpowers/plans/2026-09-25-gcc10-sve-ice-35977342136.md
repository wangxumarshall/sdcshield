# gcc-10 SVE svst1 ICE 修复(run 35977342136)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除 Multi-OS Verify workflow 中 openEuler 22.03/20.03 全部 10 个 job 的 build 失败 —— gcc-10 对 `svst1` 地址表达式内含 `& 常数` 的写法在 RTL expand 阶段 ICE(internal compiler error: Segmentation fault)。

**Architecture:** 不改任何测试语义/地址/存储行为,仅把三个 probe 文件中 `svst1` 调用的地址(及 probe_f 的向量→标量取首元素)先落入局部变量再传给 intrinsic —— 与同目录已通过编译的 probe_h(`line_idx` 先算后用)先例一致。地址操作数变成纯 SSA 名,绕开 gcc-10 aarch64 地址合法化崩溃路径;gcc-12(24.03)与 x86 不受影响。

**Tech Stack:** C++20 / arm_sve.h SVE1 intrinsics / meson+ninja / GitHub Actions(ghcr.io 离线镜像矩阵)。

**Spec:** CI 失败现场 https://github.com/wangxumarshall/sdcshield/actions/runs/35977342136(cron,main@6c76ea63,run #27)。systematic-debugging Phase 1-2 证据:
- 失败 job:22.03×5 + 20.03×5,全部死在 `Build (vendored libs + meson + ninja)`;`Upload built/ tarball` 的 "No files were found: dist/*/sdcshield-*.tar.gz" 只是连锁(无产物可传)。
- 24.03×5 全绿(gcc-12.3)。
- ICE 精确命中 PR #150(feat/sve-port-avx53)batch-5 新增的 3 个文件,且均指向 `svst1` 的地址操作数:
  - `tests/cpu/sve/core179/movbe_dump_probe_f_sve.cpp:96` — `svst1_u32(pg1, &line_window[(base / lanes) & 15], ...)`
  - `tests/cpu/sve/core179/movbe_dump_probe_g1_sve.cpp:95` — `svst1_u8(pg, (uint8_t *)(data->swapped + ((base / lanes) & 15) * slot_scale), vswapped)`
  - `tests/cpu/sve/core179/movbe_dump_probe_g2_sve.cpp:91` — `svst1_u8(pg, (uint8_t *)(data->swapped + ((base / lanes) & 15) * (128 / 4)), vswapped)`
- 对照组:同目录 8 个通过文件(a,b,c,d,e,h,x,xn)的 svst1 地址均**不含 AND**(`data->swapped + base`、`line_idx * 常数` 或裸数组);probe_h:99-100 先把索引算进局部变量 `line_idx` 再用 —— 本修复的先例。
- 全仓 grep 确认仅这 3 个文件命中 `svst1.*& 常数` 模式;ninja 停止前只编到 449/471,但剩余目标无此模式,风险清零。
- 22.03(gcc-10.3.1 系统编译器)与 20.03(gcc-toolset-10)同一 ICE、同一文件;report notice(ubuntu-latest 2026-10-19 迁移)为信息性提示,非失败,不在本计划范围。

## Global Constraints

- CLAUDE.md 规则 1:一个单元一个 patch —— 3 个文件同一根因 = 一个 commit。
- CLAUDE.md 规则 2:自验证 100% 真实 —— `ninja -C builddir` 零新警告;真实运行受影响行为(3 个 SVE probe 测试)引用真实输出;回归 `-e zstd19 -t 2000 -n 1` → `exit: pass`;x86 非回归靠检查(文件 `#if defined(__aarch64__)` 包裹,`#else` 占位分支不动)。
- CLAUDE.md 规则 3:`git branch --show-current` 确认分支后 `git commit -s`(DCO),推 feature 分支,绝不推 main。
- 测试语义零变化:存储地址、存储值、谓词、循环结构全部不变(修复只改表达式形态)。
- 本机无 gcc-10/容器运行时 → gcc-10 ICE 的真实验证 = 推分支后对该分支 workflow_dispatch Multi-OS Verify(Build 步骤无条件执行,是 ICE 的真实现场)。
- SVE 测试 VL 无关(meson.build 注释:svwhilelt 谓词批处理),本地 Kunpeng 920(VL=256)可直接运行。

---

### Task 1: 三个 probe 文件的 svst1 地址(及 probe_f 首元素)落入局部变量

**Files:**
- Modify: `tests/cpu/sve/core179/movbe_dump_probe_f_sve.cpp:89-99`
- Modify: `tests/cpu/sve/core179/movbe_dump_probe_g1_sve.cpp:91-96`
- Modify: `tests/cpu/sve/core179/movbe_dump_probe_g2_sve.cpp:89-91`

**Interfaces:**
- Consumes: 无(叶子改动)。
- Produces: 无符号变化;三个 `*_run` 行为、`DECLARE_TEST` 注册不变。后续任务依赖:这三文件可在 gcc-10 下编译通过。

- [ ] **Step 1: 修改 probe_f**

```c
            /* store swapped (向量 store/reload 路径) */
            /* PROBE F: store 在单个 cache line 内轮转 (不越行) — svst1 直打窗口 */
            {
                static uint32_t line_window[16];
                uint32_t first = 0;
                memcpy(&first, &vswapped, 4);   /* 取首元素 (编译器协助) */
                svbool_t pg1 = svwhilelt_b32((uint64_t)0, (uint64_t)1);
                /* gcc-10 (22.03/20.03 CI, run 35977342136) 对 svst1 地址式内含
                   "& 常数" 在 RTL expand ICE;地址与首元素先落入局部变量再传,
                   语义不变 (probe_h 的 line_idx 先例)。 */
                uint32_t *win = &line_window[(base / lanes) & 15];
                svst1_u32(pg1, win, svdup_u32(first));
            }
```
(替换原 96-98 行的内联地址 + `((uint32_t *)&vswapped)[0]` 强转下标;`first` 由 memcpy 定义、改后真实被用,删除 `(void)first;`。)

- [ ] **Step 2: 修改 probe_g1**

```c
            {
                const size_t slot_scale = (256 * 1024) / 4;
                /* gcc-10 (22.03/20.03 CI, run 35977342136) 对 svst1 地址式内含
                   "& 常数" 在 RTL expand ICE;地址先落入局部指针再传,
                   语义不变 (probe_h 的 line_idx 先例)。 */
                uint8_t *dst = (uint8_t *)(data->swapped + ((base / lanes) & 15) * slot_scale);
                svst1_u8(pg, dst, vswapped);
            }
```

- [ ] **Step 3: 修改 probe_g2**

```c
            /* PROBE G2: store 到 16 个不同 128B 行 — svst1 直打堆 */
            /* gcc-10 (22.03/20.03 CI, run 35977342136) 对 svst1 地址式内含
               "& 常数" 在 RTL expand ICE;地址先落入局部指针再传,
               语义不变 (probe_h 的 line_idx 先例)。 */
            uint8_t *dst = (uint8_t *)(data->swapped + ((base / lanes) & 15) * (128 / 4));
            svst1_u8(pg, dst, vswapped);
```

- [ ] **Step 4: 本地全量构建,零新警告**

Run: `ninja -C builddir` (增量即可验证该 TU;如需全量先 `ninja -C builddir clean`)
Expected: `tests/cpu/sve/libtests_sve_avx53.a.p/core179_movbe_dump_probe_{f,g1,g2}_sve.cpp.o` 编译成功,无新警告(gcc-12.3.1)。

- [ ] **Step 5: 真实运行受影响的 3 个测试**

Run: `./builddir/sdcshield -e movbe_dump_probe_f_sve -t 5000 -n 1`(g1、g2 同理)
Expected: 各自 `exit: pass`(修复不改语义,本地行为与修复前一致)。

- [ ] **Step 6: 回归 + x86 检查**

Run: `./builddir/sdcshield -e zstd19 -t 2000 -n 1` → `exit: pass`。
x86:三文件均为 `#if defined(__aarch64__)` 包裹 + `#else` 占位 skip 分支,未触碰 → 检查即非回归。

### Task 2: 提交、推送、CI 真实验证

**Files:**
- Create: 分支 `fix/gcc10-sve-st1-ice-20260925`(自 main)
- 验证: GitHub Actions workflow `multi-os-verify.yml` 对该分支 dispatch

**Interfaces:**
- Consumes: Task 1 的三个已改文件。
- Produces: feature 分支 + 绿色 CI 证据;可开 PR 合入 main。

- [ ] **Step 1: 分支 + DCO 提交 + 推送**

```bash
git branch --show-current   # 确认不在 main
git checkout -b fix/gcc10-sve-st1-ice-20260925
git add tests/cpu/sve/core179/movbe_dump_probe_f_sve.cpp \
        tests/cpu/sve/core179/movbe_dump_probe_g1_sve.cpp \
        tests/cpu/sve/core179/movbe_dump_probe_g2_sve.cpp
git commit -s -m "tests(sve): work around gcc-10 ICE on svst1 addresses containing AND ..."
git push -u origin fix/gcc10-sve-st1-ice-20260925
```

- [ ] **Step 2: 对分支 dispatch Multi-OS Verify(默认 full)**

Run: `POST /repos/wangxumarshall/sdcshield/actions/workflows/multi-os-verify.yml/dispatches {"ref":"fix/gcc10-sve-st1-ice-20260925"}`
Expected: 22.03/20.03 共 10 个 job 的 Build 步骤通过(ICE 消失),15/15 全绿。

- [ ] **Step 3: 汇报 + PR**

15/15 绿后向用户汇报,并开 PR 合入 main(标题注明 run 35977342136 根因与修法)。若 CI 仍红:回到 systematic-debugging Phase 1,带新日志重新归因,不做第二次盲改。

## Self-Review

- 覆盖:22.03/20.03 build 失败 = 3 文件 ICE → Task 1;CI 真实验证 → Task 2;upload 报错为连锁无需单独处理;report notice 非失败,明示范围外。✓
- 占位符:无 TBD/TODO;所有代码块完整可抄。✓
- 类型一致:`uint32_t *win`/`uint8_t *dst` 与 svst1_u32/svst1_u8 形参类型一致;`svdup_u32(first)` 中 `first` 为 `uint32_t`。✓
